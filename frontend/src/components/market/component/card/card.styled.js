import styled from "styled-components";
import { Card } from "react-bootstrap";

export const RCard = styled(Card)`
  min-height: 600px;
  margin-bottom: 10px;
`;
export const RImg = styled(Card.Img)`
  min-height: 253px;
`;

export const RTitle = styled(Card.Title)`
  font-size: 15px;
`;
